<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.umang.smarthome">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.project.umang.smarthome.SplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.project.umang.smarthome.DeviceList"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name="com.project.umang.smarthome.ledControl"
            android:label="LED Control">

        </activity>
        <activity
            android:name="com.project.umang.smarthome.AboutActivity"
            android:label="title_activity_about">
        </activity>

    </application>

</manifest>
